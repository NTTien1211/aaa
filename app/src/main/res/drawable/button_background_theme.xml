<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền trong suốt -->
    <solid android:color="#88FFFFFF" />
    <!-- Màu viền -->
    <stroke
        android:width="1dp"
        android:color="@color/custombtn" /> <!-- Blue -->

    <!-- Bán kính bo góc -->
    <corners android:radius="20dp" />
</shape>
